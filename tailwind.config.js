module.exports = {
  content: ["./src/**/*.{html,js}"],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif']
      },
      colors: {
        primary:'#040615',
        'to-gredient': '#6bbcff',
      }
      // Other theme customizations if needed
    },
  },
  plugins: [
    // Additional plugins if used
  ],
}

